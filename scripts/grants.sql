create table customer_users(
	email varchar(50),
    password varchar(20)
);

create table driver_users(
	email varchar(50),
    password varchar(20)
);

create table admin_users(
	email varchar(50),
    password varchar(20)
);

